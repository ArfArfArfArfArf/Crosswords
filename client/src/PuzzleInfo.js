export default class PuzzleInfo {
  constructor(frequency, name, ID, type, flags, enabled) {
    this.frequency = frequency;
    this.name = name;
    this.ID = ID;
    this.type = type;
    this.flags = flags;
    this.enabled = enabled;
  }
}
