export default class PuzzleInfo {
  constructor(frequency, name, ID, type, flags) {
    this.frequency = frequency;
    this.name = name;
    this.ID = ID;
    this.type = type;
    this.flags = flags;
  }
}
