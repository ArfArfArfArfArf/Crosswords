export default class PuzzleInfo {
  constructor(frequency, name, ID, type, flags, enabled, description) {
    this.frequency = frequency;
    this.name = name;
    this.ID = ID;
    this.type = type;
    this.flags = flags;
    this.enabled = enabled;
    this.description = description;
  }
}
